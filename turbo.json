{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": [
        "dist/**",
        "build/**"
      ]
    },
    "build-docs": {
      "outputs": [
        "dist/**",
        "build/**"
      ]
    }
  }
}
