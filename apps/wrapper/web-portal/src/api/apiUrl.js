const API_URL = {
    auth: {
        register: 'user/registration'
    },
    groundAnalysis: {
        viewForm: 'rest/getFormData'
    },
    manageUsers: {
        
    }
}

export default API_URL;